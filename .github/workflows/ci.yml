name: CI

on:
  push:
    branches: [main, master]
    tags:
      - "v[0-9]*.[0-9]*.[0-9]*"
  pull_request:
    branches: [main, master]

jobs:
  lint:
    name: Lint with luacheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Lua/LuaRocks
        run: |
          sudo apt-get update
          sudo apt-get install -y lua5.1 liblua5.1-0-dev luarocks libsodium-dev
      - name: Install luacheck
        run: sudo luarocks install luacheck

      - name: Run luacheck
        run: luacheck --no-unused-args .

      - name: Update lint badge
        uses: schneegans/dynamic-badges-action@v1.9.0
        if: always()
        with:
          auth: ${{ secrets.GIST_SECRET }}
          gistID: ${{ secrets.GIST_ID}}
          filename: lint-badge.json
          label: Lint with luacheck
          message: ${{ job.status }}
          color: ${{ job.status == 'success' && 'green' || 'red' }}

  test:
    name: Run busted tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Lua/LuaRocks
        run: |
          sudo apt-get update
          sudo apt-get install -y lua5.1 liblua5.1-0-dev luarocks libsodium-dev
      - name: Install lit/luvi/luvit
        run: |
          curl -L https://github.com/luvit/lit/raw/master/get-lit.sh | sh
          sudo install lit luvi luvit /usr/local/bin/
      - name: Install luvit dependencies
        run: lit install luvit/coro-http luvit/coro-websocket luvit/secure-socket luvit/json

      - name: Install busted
        run: sudo luarocks install busted

      - name: Run busted tests
        run: |
          LUA_PATH="./spec/?.lua;./deps/?.lua;./deps/?/init.lua;;" \
          LUAROCKS_SYSCONFDIR='/etc/luarocks' \
          exec luvit -e 'package.path="/usr/share/lua/5.1/?.lua;/usr/share/lua/5.1/?/init.lua;"..package.path;package.cpath="/usr/lib/lua/5.1/?.so;"..package.cpath;local k,l,_=pcall(require,"luarocks.loader") _=k and l.add_context("busted","2.3.0-1");arg={"-p","_spec.lua","-r","spec"};dofile("/usr/lib/luarocks/rocks-5.1/busted/2.3.0-1/bin/busted")' "$@"

      - name: Update test badge
        uses: schneegans/dynamic-badges-action@v1.9.0
        if: always()
        with:
          auth: ${{ secrets.GIST_SECRET }}
          gistID: ${{ secrets.GIST_ID}}
          filename: test-badge.json
          label: Run busted tests
          message: ${{ job.status }}
          color: ${{ job.status == 'success' && 'green' || 'red' }}

  rockspec:
    name: Validate rockspec
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Lua/LuaRocks
        run: |
          sudo apt-get update
          sudo apt-get install -y lua5.1 liblua5.1-0-dev luarocks libsodium-dev
      - name: Validate rockspec
        run: luarocks lint discord-lua-scm-0.rockspec

      - name: Update rockspec badge
        uses: schneegans/dynamic-badges-action@v1.9.0
        if: always()
        with:
          auth: ${{ secrets.GIST_SECRET }}
          gistID: ${{ secrets.GIST_ID}}
          filename: rockspec-badge.json
          label: Validate rockspec
          message: ${{ job.status }}
          color: ${{ job.status == 'success' && 'green' || 'red' }}